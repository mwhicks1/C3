# C3

Use following to run example:

```
dune exec -- src/main.exe -f samples/hello-world.c
```

# TODO

- Add test cases
- Run testcases too (don't just compile them), and compare to expected output
- Polymorphic types
- Set up and run bigger benchmarks

# Limitations

- The `count`, `bounds`, etc. keywords can oftentimes be used as variables but still might break in some situations
